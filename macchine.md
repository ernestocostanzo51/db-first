# Nome Tabella: Macchine

 -ID AUTO_INCREMENT,UNIQUE,NOT NULL [PK]
 -marca macchina VARCHAR(20),NOT NULL
 -nome macchina VARCHAR(20),NOT NULL
 -data di immatricolazione (DATE) NOT NULL
 -targa macchina VARCHAR(7), UNIQUE, NOT NULL
 -costo DECIMAL(10, 2), NOT NULL
 -descrizione TEXT NULL
